<% content_for(:title, @user.name)%>
<%= render "profiles/photographer_profile" if current_user.type == "Photographer"%>
<%= render "profiles/designer_profile" if current_user.type == "Designer"%> 
<%= render "profiles/model_profile" if current_user.type == "Model"%> 
